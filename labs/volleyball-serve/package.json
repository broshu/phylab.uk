{
  "name": "phylab-volleyball-serve",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PhyLab · volleyball serve projectile lab. Static site — this file only exists so the node self-tests can import the ES modules.",
  "scripts": {
    "test": "node tests/check.mjs && node tests/i18n.mjs && node tests/ai-coach.mjs && node tests/smoke.mjs && node tests/chinese-smoke.mjs && node tests/coach-flow.mjs"
  },
  "dependencies": {
    "katex": "^0.18.4"
  }
}
