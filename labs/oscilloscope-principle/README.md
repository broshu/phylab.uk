# Oscilloscope Principle Demo

Open `index.html` to adjust X and Y deflection voltages and observe the ion spot on the fluorescent screen.

The folder follows the same structure as `games/electric-field-lines/`:

- `index.html`
- `css/style.css`
- `js/main.js`
